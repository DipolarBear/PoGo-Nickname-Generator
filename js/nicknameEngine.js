export function generateNickname(input) {
  /*
    input = {
      isPokeGenie: boolean,
      locality: string | null,        // local friends only
      country: string | null,         // PG only
      state: string | null,           // PG only
      friendDate: { y, m, d },
      bestFriendDate: { y, m, d } | null,
      foreverFriendDate: { y, m } | null,
      status: string                  // "", "X", "XY"
    }
  */

  // TEMP: stub output so we can test wiring
  return "TEST_OK";
}
